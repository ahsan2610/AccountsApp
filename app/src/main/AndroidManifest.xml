<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="techsol.wajeeh.smartaccounts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".gui.MainActivity" />
        <activity
            android:name=".gui.SplashScreen"
            android:label="@string/app_name"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.Registration" />
        <activity android:name=".login.Login" />
        <activity android:name=".gui.acc_detail" />
        <activity android:name=".gui.newTransation" />
        <activity android:name=".gui.add_new_account"></activity>
    </application>

</manifest>